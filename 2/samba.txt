samba
    -공유 폴더 제공 서비스
    -익명 공유, 사용자 지정 공유, 그룹 지정 공유의 종류가 있음
    -정보
        패키지  :samba
        데몬    :smb
        방화벽  : service=samba
        설정파일: /etc/samba/smb.conf

    -설치
        yum -y install samba-*

        apt-get -y install samba
        yum install samba